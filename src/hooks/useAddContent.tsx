
interface Content{
    title: string;
    content?: string;
    link?: string;
    tags?: string[];
    type: string;
}
const useAddContent = () => {
    const addContent = async (content : Content)=>{

    }
}

export default useAddContent